package components

templ sidebarTemplate(c *Sidebar, props SidebarProps) {
	<div class="filters">
		<h3>Filters</h3>
		<form>
			<div class="filter-group">
				<label class="filter-label">Status</label>
				<div class="filter-options">
					<label class="filter-option">
						<input
							type="radio"
							name="status"
							value=""
							checked?={ props.CurrentStatus == "" }
							{ c.WireFilter(props)... }
							hx-target=".filters"
							hx-swap="outerHTML"
						/>
						<span>All</span>
					</label>
					<label class="filter-option">
						<input
							type="radio"
							name="status"
							value="pending"
							checked?={ props.CurrentStatus == "pending" }
							{ c.WireFilter(props)... }
							hx-target=".filters"
							hx-swap="outerHTML"
						/>
						<span>Pending</span>
					</label>
					<label class="filter-option">
						<input
							type="radio"
							name="status"
							value="completed"
							checked?={ props.CurrentStatus == "completed" }
							{ c.WireFilter(props)... }
							hx-target=".filters"
							hx-swap="outerHTML"
						/>
						<span>Completed</span>
					</label>
				</div>
			</div>
			<button
				type="button"
				class="clear-btn"
				{ c.WireClear(props)... }
				hx-target=".filters"
				hx-swap="outerHTML"
			>
				Clear Filters
			</button>
		</form>
	</div>
}
